// Script to create circular favicon
// Run: node create-circular-favicon.js

const fs = require('fs');
const path = require('path');

// Note: This requires sharp or canvas package
// For now, we'll use the SVG approach which works in modern browsers

console.log('Circular favicon created as SVG. For PNG version, use the HTML tool in public folder.');
